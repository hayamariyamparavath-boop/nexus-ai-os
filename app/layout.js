export const metadata = {
  title: 'NEXUS AI OS',
  description: 'AI Powered OS Interface',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
